(function(sx, $, _)
{
    sx.createNamespace('classes.toolbar', sx);

    /**
     * @type {void|*|Function}
     */
    sx.classes.toolbar.Window  = sx.classes._Window.extend({});
})(sx, sx.$, sx._);